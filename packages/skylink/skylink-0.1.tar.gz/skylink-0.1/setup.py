from setuptools import setup

setup(name='skylink' , 
      description='Chatbot link',
      version=0.1,
      author='Caique santos carvalho',
      packages=['skylink'],
      zip_safe=False)
