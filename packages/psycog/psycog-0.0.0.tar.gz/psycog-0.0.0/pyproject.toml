[tool.poetry]
name = "psycog"
version = "0.0.0"
description = ""
authors = ["Sam Mathias"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
