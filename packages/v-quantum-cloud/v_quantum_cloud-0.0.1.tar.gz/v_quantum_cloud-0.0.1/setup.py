from setuptools import setup

# Package metadata
name = 'v_quantum_cloud'
version = '0.0.1'
description = 'V-Quantum cloud library'
  
# Package dependencies
dependencies = [    
]
  
# Package setup
setup(
    name=name,
    version=version,
    description=description,    
    install_requires=dependencies
)
#setup()
