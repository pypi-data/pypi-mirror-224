from setuptools import setup, find_packages

setup(
    name='gui-utils',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Lista de dependências necessárias para o seu pacote
    ],
)
