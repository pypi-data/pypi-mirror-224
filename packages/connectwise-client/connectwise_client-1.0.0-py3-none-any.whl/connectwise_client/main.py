"""Module where the application starts."""


def hello(my_name: str) -> str:
    """Proves the library functions can be called properly.

    parameters:
        my_name: name to be processed.

    returns:
        hello name.
    """
    return f"hello {my_name}"
