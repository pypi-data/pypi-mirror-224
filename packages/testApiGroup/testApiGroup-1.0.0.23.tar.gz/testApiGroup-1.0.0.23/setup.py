# encoding: utf-8 
from setuptools import setup, find_packages

setup(
    name='testApiGroup',
    version='1.0.0.23',  # 替换为您的版本号
    description='${Description}',
    author='dyc',
    author_email='your@email.com',
    packages=['testApiGroup'],
    install_requires=['requests']
)