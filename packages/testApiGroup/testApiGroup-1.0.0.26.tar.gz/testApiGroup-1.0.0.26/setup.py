# encoding: utf-8 
from setuptools import setup, find_packages

setup(
    name='testApiGroup',
    version='1.0.0.26',  # 替换为您的版本号
    description='${Description}',
    author='dyc',
    author_email='your@email.com',
    packages=['导入的项目五', '导入的项目四'],
    install_requires=['requests']
)